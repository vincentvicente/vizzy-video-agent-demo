[project]
name = "vizzy-infra"
version = "0.1.0"
description = "AWS CDK infrastructure for Vizzy Studio"
requires-python = ">=3.11"
dependencies = [
    "aws-cdk-lib>=2.150.0",
    "constructs>=10.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
